# S03 Day 2｜三大 Server 契约边界｜运行时证据采集

## 1. 今日定位

Sprint 2 解决的是“谁在调度”，核心结论是：

- `bt_navigator` 是整条导航任务的流程组织者
- BT 负责流程组织
- action 负责跨节点任务调用与反馈
- lifecycle 负责节点状态管理

Sprint 3 开始解决的是：

**被调度的三个核心 server——`planner_server`、`controller_server`、`behavior_server`——各自到底负责什么，它们之间的职责边界在哪里。**

S03 Day 1 做的是静态边界整理。  
S03 Day 2 不再停留在口头理解，而是要用运行时证据把这些边界钉实。

---

## 2. 今日核心问题

今天只回答四个问题：

1. `planner_server` 和 `planner plugin` 的边界，运行时怎么证明
2. `controller_server` 和 `controller plugin` 的边界，运行时怎么证明
3. `behavior_server` 负责哪些动作，哪些恢复动作不归它管
4. `bt_navigator / BT node / server / plugin` 四层，运行时各卡在哪一层

---

## 3. 今日明确不做什么

今天不做以下内容：

- 不改 BT XML
- 不改参数
- 不做恢复实验
- 不深挖源码
- 不写复杂表格

今天只做一件事：

**用 CLI 和运行时对象，把昨天的边界理解变成可验证的证据。**

---

## 4. 今日采集到的运行时证据

### 4.1 导航入口与总调度者

通过：

    ros2 action info /navigate_to_pose

确认：

- `/navigate_to_pose` 的 action server 是 `/bt_navigator`

这说明：

**导航任务入口先到 `bt_navigator`，它是导航流程的总调度者。**

---

### 4.2 默认 BT 中的关键任务节点

通过：

    ros2 param get /bt_navigator default_nav_to_pose_bt_xml

拿到当前行为树路径，并进一步检查关键节点，确认默认树中存在：

- `ComputePathToPose`
- `FollowPath`
- `Spin`
- `Wait`
- `BackUp`

这说明：

- `bt_navigator` 通过 BT 组织任务流程
- 规划、控制、恢复相关任务，在树里都有对应节点
- BT 负责“流程组织”，不是算法本体

---

### 4.3 planner_server 与 planner plugin 的边界

通过：

    ros2 param get /planner_server planner_plugins
    ros2 param get /planner_server GridBased.plugin

确认：

- `planner_plugins = ['GridBased']`
- `GridBased.plugin = nav2_navfn_planner/NavfnPlanner`

这说明：

- `planner_server` 暴露的是规划能力入口与插件装载层
- `GridBased` 是 planner plugin 的 ID
- `nav2_navfn_planner/NavfnPlanner` 是具体插件实现

因此可以确认：

**`planner_server` 是规划能力入口，planner plugin 是挂在它下面的具体规划实现。**

换句话说：

- server 不是算法本体
- plugin 才是具体实现

---

### 4.4 controller_server 与 controller plugin 的边界

通过：

    ros2 param get /controller_server controller_plugins
    ros2 param get /controller_server FollowPath.plugin
    ros2 param list /controller_server | grep '\.plugin$'

确认：

- `controller_plugins = ['FollowPath']`
- `FollowPath.plugin = dwb_core::DWBLocalPlanner`
- `/controller_server` 下还挂有：
  - `general_goal_checker.plugin`
  - `progress_checker.plugin`

这说明：

- `controller_server` 不是单一控制算法本体
- 它是控制能力入口 / 装载层
- `FollowPath` 对应具体控制插件实现
- goal checker、progress checker 也挂在 `controller_server` 下面，共同构成控制运行链的一部分

因此可以确认：

**`controller_server` 是控制能力入口，controller plugin 是它内部装载的具体控制实现。**

---

### 4.5 behavior_server 与行为动作的边界

通过：

    ros2 param list /behavior_server | grep -E 'plugin|plugins'
    ros2 action list | grep -E 'spin|wait|backup'

确认：

- `/behavior_server` 下存在：
  - `behavior_plugins`
  - `spin.plugin`
  - `wait.plugin`
  - `backup.plugin`
- 系统中存在行为动作 action：
  - `/spin`
  - `/wait`
  - `/backup`

这说明：

**`behavior_server` 负责的是 `spin / wait / backup` 这类行为动作型恢复。**

---

### 4.6 behavior_server 与 Clear*Costmap 的边界

通过：

    ros2 service list | grep -E 'clear.*costmap|costmap.*clear'

确认系统中的清图入口是：

- `/global_costmap/clear_around_global_costmap`
- `/global_costmap/clear_entirely_global_costmap`
- `/global_costmap/clear_except_global_costmap`
- `/local_costmap/clear_around_local_costmap`
- `/local_costmap/clear_entirely_local_costmap`
- `/local_costmap/clear_except_local_costmap`

这些入口全部表现为 **service**，不是 action。

因此可以确认：

**`Clear*Costmap` 属于清图 service，不属于 `behavior_server` 的行为动作范畴。**

这条边界很重要，因为它说明：

- 不是所有“恢复”都归 `behavior_server`
- `behavior_server` 只覆盖行为动作型恢复
- 清图类恢复走的是另一套 service 接口

---

### 4.7 成功主线中的 server 参与情况

本次简单成功 case 的日志表现为：

- `bt_navigator: Begin navigating ...`
- `controller_server: Received a goal, begin computing control effort`
- `controller_server: Passing new path to controller`
- `controller_server: Reached the goal!`
- `bt_navigator: Goal succeeded`

本次日志中没有出现：

- `Running spin`
- `Running wait`
- `Running backup`

这说明：

**在常规成功主线中，`behavior_server` 不是必经节点。**  
它只有在主线失败、切入恢复 / 特殊行为阶段时才可能参与。

---

## 5. 四层卡位关系（今天最核心的结论）

基于今天的运行时证据，可以把四层关系整理成下面这张“人话版骨架”：

### 第一层：bt_navigator
- 接收 `NavigateToPose` goal
- 负责整条导航任务的流程组织

### 第二层：BT node
- 是行为树里的任务节点 / 控制节点
- 例如：
  - `ComputePathToPose`
  - `FollowPath`
  - `Spin`
  - `Wait`
  - `BackUp`

### 第三层：server
- 是 ROS 侧真正承接能力请求的入口
- 例如：
  - `planner_server`
  - `controller_server`
  - `behavior_server`

### 第四层：plugin
- 是挂在 server 下的具体实现
- 例如：
  - `nav2_navfn_planner/NavfnPlanner`
  - `dwb_core::DWBLocalPlanner`
  - `spin / wait / backup` 对应行为插件

一句话总结就是：

**`bt_navigator` 管流程，BT node 是树里的任务节点，server 是能力入口，plugin 是具体实现。**

---

## 6. 今日阶段性结论

今天至少确认了六件事：

1. `planner_server` 是规划能力入口，planner plugin 是挂在它下面的具体规划实现
2. `controller_server` 是控制能力入口，不等于某一个控制算法本体；它下面还挂有控制插件、goal checker、progress checker
3. `behavior_server` 负责 `spin / wait / backup` 这类行为动作型恢复
4. `Clear*Costmap` 属于清图 service，不属于 `behavior_server`
5. `/navigate_to_pose` 的 action server 是 `bt_navigator`，说明导航任务先进入调度层
6. `bt_navigator -> BT node -> server -> plugin` 是四层不同对象，不能混讲

---

## 7. 今日最短口径

今天收口后，最短可以这样讲：

`NavigateToPose` goal 先到 `bt_navigator`，由它通过 BT 组织任务流程。BT 中的 `ComputePathToPose`、`FollowPath`、`Spin / Wait / BackUp` 是树里的任务节点；真正承接能力请求的是 `planner_server`、`controller_server`、`behavior_server`；而具体能力实现则由各自挂载的 plugin 提供。`behavior_server` 只覆盖行为动作型恢复，不等于所有恢复动作入口，`Clear*Costmap` 这类恢复走的是清图 service。

---

## 8. 今天的价值

今天最大的价值不是学了更多名词，而是把昨天的“静态理解”变成了“运行时证据”：

- 不是嘴上说 server 和 plugin 不一样
- 而是通过参数和对象关系证明它们确实不一样

- 不是嘴上说 `behavior_server` 不等于所有恢复动作
- 而是通过 action 与 service 的入口差异证明了这条边界

所以，S03 Day 2 的意义在于：

**把三大 server 的契约边界，从概念层推进到了运行时证据层。**

---

## 9. 明天要接什么

如果继续按主线推进，S03 Day 3 最自然要接的是：

**把今天的运行时证据，进一步压成“主线调用关系 + 失败时切换关系”的结构化说明。**

也就是继续回答：

- planner / controller / behavior 三者在成功主线中谁先谁后
- 失败后从主线切到恢复时，边界如何变化
- BT node 和 server 的关系，如何从“静态边界”过渡到“调用链”

今天先到这里，不继续展开。